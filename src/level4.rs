const LEVEL: &str = "level4";

#[derive(Copy, Clone, PartialEq, Eq)]
pub enum Entity {
    Coin,
    Ghost,
    Pacman,
    Space,
}

struct Game {

}

impl Game {
    pub fn new(path: &str) -> Self {
        
    }
}